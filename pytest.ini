[pytest]
testpaths = tests
addopts = -v --tb=short --html=reports/report.html --self-contained-html
markers =
    smoke: Quick sanity checks
    regression: Full regression suite
    healthcare: Healthcare site tests (targeting real demo sites)
    security: Security and privacy validation tests
